#特殊文件夹
**/.tmp/
:结束运行时总是可以删除，包含两类：
1.测试输出的临时文件，能确认测试出的问题，删掉后可重测试得到
2.“独立项目”（如小红薯聚合）会把引用的常用代码复制到这
每次运行时会自动复制，
运行过程中不知道是否能删除，一般也都不会需要删除

*/.log/
:日志专用，可能有特殊情况的日志，确认不再需要则可删

*/output/
:不是测试的，而是正式的输出，但也可以随时重生成，随时删除
目前在“输入法自定词库”和“zd2md”两个项目使用
其实可以合并到.tmp，区分“output”以对用户友好

*/images/
*/build/
:这两个是Auto.jsPro的打包工具用的