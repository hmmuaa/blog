import'#g'
const{}={}
,a=`\
#班会
周三下午2:30开班会，有以下主要内容。
1.值日安排
2.征集图书分享
.实事讨论
>刘慈心《流浪地球2》上映
国产大片里程碑
>不会用AI的程序员不是好程序员
~~班会时间可能被数学占用
##值日生
-小明
-小强
>会后记录`
/*不使用空格，开头“#、-、1.”后不用空格
	markor中不用处理“>”
*/
eq(a.match(
		/(?<=\n)(#{1,6}|[-]|[1-9]\.)(?=.)/g
		),['1.','2.','##','-','-'])
///开头两点表示自动排序号
eq(a.match(
		/\n\.(?=\S)/g
		),['\n.'])

///compact格式，个人习惯省略不必要的空行
eq(a.match(
		/(?<=>([^#].+\n)+)>/g
		),['>'])

p(a.split('\n').filter(a=>a[0]=='#'))