B站视频有下载网站 但不支持720p
学ukulele 720p较合适 看得清按弦
最方便的是离线缓存后 用[BiliDownOut](
github.com/10miaomiao/bili-down-out)导出
只需授权文件访问即可

BiliDownOut不支持合集 只能手动
1.用mixp打开`tv.danmaku.bili/download`找到
.可文件夹复制出来
.选video.m4s分享到ffmpeg 在ff选“2 files”
.切换回mx把audio.m4s分享到ff
.正确填写两个文件 不能选会导致bug清除 要填写
.输出默认
.命令选“-c copy” 执行